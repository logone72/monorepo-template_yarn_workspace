export default {
  // 한 줄당 최대 글자 수
  printWidth: 120,
  // 객체나 배열의 마지막 요소 뒤에 항상 쉼표를 붙임
  trailingComma: 'all',
  // 들여쓰기 크기
  tabWidth: 2,
  // 문장 끝에 세미콜론을 붙임
  semi: true,
  // 문자열에 작은따옴표 사용
  singleQuote: true,
  // 객체 리터럴의 중괄호 주위에 공백을 넣음
  bracketSpacing: true,
  // HTML의 마지막 '>'를 다음 줄로 내림
  bracketSameLine: false,
  // JSX의 마지막 '>'를 다음 줄로 내림
  jsxBracketSameLine: false,
  // 화살표 함수의 매개변수를 항상 괄호로 감싸기
  arrowParens: 'always',
  // 탭 대신 스페이스 사용
  useTabs: false
}
